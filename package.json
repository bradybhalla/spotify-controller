{
  "name": "spotify-controller",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "install-all": "npm install && cd client && npm install",
    "authenticate": "source scripts/authenticate.sh",

    "client-dev": "cd client; npm run dev",
    "client-build": "cd client; npm run build",
    "client-preview": "cd client; npm run preview",

    "server-build": "cd server; tsc",

    "dev": "source scripts/build.sh && node dist/index.js; source scripts/clean.sh",
    "build": "source scripts/build.sh",
    "clean": "source scripts/clean.sh"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "express": "^4.18.2",
    "socket.io": "^4.6.1"
  },
  "devDependencies": {
    "@types/express": "^4.17.17",
    "typescript": "^4.9.5"
  }
}
